<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <meta name="author" content="Kostin Vladislav"/>
    <meta name="description" content="Содержит форму отправки для ."/>
    <title>Посещаемость</title>
    {{>linkscripts}}
    {{>styles}}
</head>
<body>
    <div class="container-fluid h-100">
        <div class="row h-100">
            {{>sidebar}}
            <div class="row h-100 main {{#equal sidebar_d 0}}main-full-screen{{else}}main-n-full-screen{{/equal}}">
                {{>applogo}}
                <div class="row justify-content-center col-12 mx-0 px-0 application">
                    {{>header}}
                    <div class="col-12 row justify-content-center align-items-start align-content-start row mx-0 px-0 app-window t-attendance-info">
                        <div class="col-12 h-of row">
                             {{>backbutton}} <h1 class="col-xl-10 col-12 text-right">Посещаемость</h1> 
                        </div>
                        <div class="col-12">
                            <h2 class="text-center h-section">{{#datenormalise att_date "D-M-Y"}}{{/datenormalise}}</h2>
                        </div>
                        <div class="col-xl-6 col-12 row justify-content-center m-0 p-0">
                            <div class="col-12">
                                <h3 class="text-center">Присутствующие</h3>
                            </div>
                            {{#each present}}
                                <div class="col-10 m-3 t-abs-student-block row align-items-center justify-content-start stylise-block" id="{{student_id}}">
                                    <div class="col-5">
                                        <img src="{{#base64 (imgnotnull user_photo user_sex)}}{{/base64}}" alt="">
                                    </div>
                                    <div class="col-7">
                                        <p>{{user_sur_name}} {{user_name}} {{user_mid_name}}</p>
                                    </div>
                                </div>
                                {{else}}
                                <div class="col-12">
                                    <h4>Отсутствуют</h4>
                                </div>
                            {{/each}}
                        </div>
                        <div class="col-xl-6 col-12 border-left border-dark row justify-content-center m-0 p-0">
                            <div class="col-12">
                                <h3 class="text-center">Отсутствующие</h3>
                            </div>
                            {{#each absented}}
                                <div class="col-10 m-3 t-abs-student-block row align-items-center justify-content-start stylise-block" id="{{student_id}}">
                                    <div class="col-5">
                                        <img src="{{#imgnotnull user_photo user_sex}}{{/imgnotnull}}" alt="">
                                    </div>
                                    <div class="col-7">
                                        <p>{{user_sur_name}} {{user_name}} {{user_mid_name}}</p>
                                    </div>
                                </div>
                                {{else}}
                                <div class="col-12">
                                    <h4>Отсутствуют</h4>
                                </div>
                            {{/each}}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>  
</body>
</html>