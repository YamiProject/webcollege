<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <meta name="author" content="Kostin Vladislav"/>
    <meta name="description" content="Содержит форму отправки для ."/>
    <title>Посещаемость</title>
    {{>linkscripts}}
    {{>styles}}
</head>
<body>
    <div class="container-fluid h-100">
        <div class="row h-100">
            {{>sidebar}}
            <div class="row h-100 main {{#equal sidebar_d 0}}main-full-screen{{else}}main-n-full-screen{{/equal}}">
                {{>applogo}}
                <div class="row justify-content-center col-12 mx-0 px-0 application">
                    {{>header}}
                    <div class="col-12 row justify-content-center align-items-center align-content-start row mx-0 px-0 app-window t-attendance">
                        <div class="col-12 h-of">
                            <h1 class="text-right col-12">Посещаемость</h1> 
                        </div>
                        <div class="col-12 row justify-content-center pb-5">
                            <div class="col-9 row align-content-start" id="t-attendance-list">
                                <div class="col-12">
                                    <h2 class="text-center h-section">Отчёты</h2>
                                </div>
                                {{#each at_list}}
                                    <div class="col-xl-4 col-10 row m-2 p-4 stylise-block" id="att-{{attendance_id}}">
                                        <div class="col-12">
                                            <h2>Дата: {{#datenormalise attendance_date "D-M-Y"}}{{/datenormalise}}</h2>
                                        </div>
                                        <div class="col-12">
                                            <span>Присутствующих: {{attendance_present}}</span>
                                        </div>
                                    </div>
                                {{/each}}
                            </div>
                            <div class="col-3 row justify-content-center align-items-center align-content-center t-fixed-attendance-block">
                                <div class="col-12 row justify-content-end">
                                    <button class="btn stylise-block col-12" id="t-my-group-na-link">Составить</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>  
</body>
</html>